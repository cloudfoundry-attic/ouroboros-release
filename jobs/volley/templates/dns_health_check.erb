#!/bin/bash

# Use volley's cups endpoint port
lsof -i ':<%= p("volley.cups.port") %>'

# http://www.consul.io/docs/agent/checks.html
if [ $? -ne 0 ]; then
  exit 2 # Exit higher than 1 to ensure service is registered as 'critical'
fi
