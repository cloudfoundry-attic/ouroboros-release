set -e -x

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.7)
export PATH=$GOROOT/bin:$PATH

cd ouroboros

export GOPATH=$PWD

go install github.com/cloudfoundry-incubator/ouroboros
cp -a $PWD/bin/ouroboros ${BOSH_INSTALL_TARGET}
